logging:
  level:
    org:
      hibernate: INFO
      springframework:
        web: INFO
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: sd4_c4t4l0g
    url: jdbc:postgresql://172.16.41.107:5151/sda_catalog
    username: sda_catalog
  devtools:
    add-properties: true
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        id:
          uuid:
            generator: org.hibernate.id.UUIDGenerator
        jdbc:
          lob:
            non_contextual_creation: true
  mvc:
    validation: true
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
#keycloak:
#  realm: sda-catalogue
#  resource: sda-catalogue
#  auth-server-url: http://localhost:8180/auth
#  use-resource-role-mappings: true
#  ssl-required: none
#  bearer-only: true
#  credentials:
#    secret: uPcTmgTZe5QWiE7odAG22S2L2bzhpGAy

